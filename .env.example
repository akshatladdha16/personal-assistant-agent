# --- Core ---
ENVIRONMENT=dev

# --- LLM Selection ---
# Options: openai, ollama
LLM_PROVIDER=openai

# --- OpenAI ---
# Required if LLM_PROVIDER=openai
OPENAI_API_KEY=sk-...

# --- Ollama ---
# Required if LLM_PROVIDER=ollama
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=llama3

# --- Supabase ---
# Project URL (https://xyzcompany.supabase.co)
SUPABASE_URL=https://your-project.supabase.co

# Service role or anon key with insert/read access
SUPABASE_KEY=your-service-role-or-anon-key

# Optional: table name override (defaults to "resources")
SUPABASE_RESOURCES_TABLE=resources

# --- Embeddings ---
EMBEDDING_PROVIDER=openai
EMBEDDING_MODEL=text-embedding-3-small
EMBEDDING_DIMENSIONS=1536
EMBEDDING_MATCH_THRESHOLD=1.0

# --- Telegram Bot (optional) ---
TELEGRAM_BOT_TOKEN=123456:replace-me
# Telegram user ID allowed to approve pairing requests
TELEGRAM_ADMIN_ID=12345678
TELEGRAM_POLL_INTERVAL=0.5
TELEGRAM_PAIRING_CODE_TTL_SECONDS=3600
TELEGRAM_PAIRING_PENDING_LIMIT=3
# Telegram-related env vars explanation and how to get them
# TELEGRAM_BOT_TOKEN=123456:bot-token-from-BotFather
# TELEGRAM_ADMIN_ID=123456789
# TELEGRAM_POLL_INTERVAL=0.5
# TELEGRAM_PAIRING_CODE_TTL_SECONDS=3600
# TELEGRAM_PAIRING_PENDING_LIMIT=3
# TELEGRAM_PAIRING_STORAGE_DIR=var/pairing   # optional override
# - TELEGRAM_BOT_TOKEN: the BotFather token you get when creating your bot; required to start the transport.
# - TELEGRAM_ADMIN_ID: the single Telegram user ID allowed to run /pairing commands (use @userinfobot to find yours).
# - TELEGRAM_POLL_INTERVAL: how often (seconds) the bot long-polls Telegram. Higher values reduce polling frequency; defaults to 0.5.
# - TELEGRAM_PAIRING_CODE_TTL_SECONDS: how long a pairing code is valid before it expires (default 1 hour).
# - TELEGRAM_PAIRING_PENDING_LIMIT: max number of pending pairing requests kept at once (default 3). Extra requests are blocked until older ones are resolved/expired.
# - TELEGRAM_PAIRING_STORAGE_DIR: path where pending.json and allowlist.json are written. Defaults to var/pairing; change it if you want the store elsewhere.
