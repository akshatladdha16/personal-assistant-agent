# --- Core ---
ENVIRONMENT=dev

# --- LLM Selection ---
# Options: openai, ollama
LLM_PROVIDER=openai

# --- OpenAI ---
# Required if LLM_PROVIDER=openai
OPENAI_API_KEY=sk-...

# --- Ollama ---
# Required if LLM_PROVIDER=ollama
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=llama3

# --- Notion ---
# Internal integration token (Secrets -> Internal Integrations)
NOTION_API_KEY=secret_notion_token

# Resource database ID (open the database, copy the UUID from the URL)
NOTION_RESOURCE_DATABASE_ID=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Optional: parent page if you plan to create databases dynamically
NOTION_DEFAULT_PARENT_PAGE_ID=
