# --- Core ---
ENVIRONMENT=dev

# --- LLM Selection ---
# Options: openai, ollama
LLM_PROVIDER=openai

# --- OpenAI ---
# Required if LLM_PROVIDER=openai
OPENAI_API_KEY=sk-...

# --- Ollama ---
# Required if LLM_PROVIDER=ollama
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=llama3

# --- Supabase ---
# Project URL (https://xyzcompany.supabase.co)
SUPABASE_URL=https://your-project.supabase.co

# Service role or anon key with insert/read access
SUPABASE_KEY=your-service-role-or-anon-key

# Optional: table name override (defaults to "resources")
SUPABASE_RESOURCES_TABLE=resources

# --- WhatsApp Bridge ---
# Base URL where the local WPPConnect server listens
WPPCONNECT_BASE_URL=http://localhost:21465

# Session name you configured when starting WPPConnect
WPPCONNECT_SESSION=personal-agent

# Bearer token generated via /generate-token on WPPConnect
WPPCONNECT_TOKEN=wppconnect:$2b$...

# Optional shared secret expected in the webhook header X-WPPConnect-Secret
# WPPCONNECT_WEBHOOK_SECRET=change-me
